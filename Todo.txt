Screen的Update消息处理，初步想要用一个优先级队列做记录
Interface具体实现切到EasyX上
想办法对于测试开放类的私有属性

整理所有的文件，测试和makefile都重新编写
规范好对外接口，尽可能减少由于代码改动带来的测试变化
性质保证尽量交给Assert做，从测试中独立出去
所有用户可能会用到的函数一定要写Assert，并且用前件和后件的形式
模块中重要性高的函数，全部写Assert，并且用前件和后件的形式（比如涉及内存管理的函数）
前件和后件用空行和其他代码分隔开
测试请从“我需要用这个类干什么”的角度出发去写
把测试目标写在Test文件的一开头
把测试目标对应的函数名写在写在测试目标下面
注意，测试永远只针对你要用到什么功能，不要针对一些内部性质

Makefile
1.生成.o文件的项目请写在最后，并且只负责生成.o项目
生成.o文件的项不要写依赖文件
冒号举例目标名称有一个空格
2.若有多个.o需要生成，请采取unitTest，myTest，debug，Example的顺序编写
3.测试命令采用unitTest，memUnitTest，helUnitTes等形式编写
4.把所有的测试命令写到run里面，保证顺序不变
5.clean命令写在run命令之后
6.多个依赖文件的，请保证一行一个依赖文件，顺序是Array，Dynamic，Senaphore，Random，Future，MailBox；Interface，Screen，Controller的反序（越后面越基础）

采用DiagHelper提供的Assert
所有的断言前面都是实际值，后面都是期望值
